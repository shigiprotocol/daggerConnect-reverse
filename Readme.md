# ⚙️ DaggerConnect

<div align="center">
**ریورس تانل قدرتمند**

[ویژگی‌ها](#-ویژگیها) • [نصب سریع](#-نصب-سریع) • [مستندات](#-راهنمای-استفاده) • [مثال‌ها](#-مثالها) • [پیکربندی](#-پیکربندی-پیشرفته)

</div>

<div align="center">

🔐 کاملا رایگان V1.1.3
</div>

## 📖 فهرست مطالب

- [معرفی](#-معرفی)
- [ویژگی‌ها](#-ویژگیها)
- [نصب سریع](#-نصب-سریع)
- [راهنمای استفاده](#-راهنمای-استفاده)
- [مثال‌ها](#-مثالها)
- [پیکربندی پیشرفته](#-پیکربندی-پیشرفته)
- [بنچمارک](#-بنچمارک-و-عملکرد)
---

## 🎯 معرفی

یک راهکار حرفه‌ای برای ایجاد تونل‌های امن و پرسرعت معکوس بین سرورهاست که با بهره‌گیری از آخرین فناوری‌های شبکه، امکان انتقال ترافیک با کمترین تاخیر را فراهم می‌کند. این سیستم به‌طور خاص برای محیط‌های حساس به تاخیر و نیازمند امنیت بالا طراحی شده است.

### 🔥 چرا DaggerConnect?

✅ **سرعت بالا** - با استفاده از SMUX و KCP  
✅ **چند پروتکل** - TCP, KCP, WebSocket, WSS  
✅ **UDP Support** - پشتیبانی کامل از UDP (مناسب WireGuard, QUIC, OpenVPN ...)  
✅ **Auto-reconnect** - اتصال مجدد خودکار  
✅ **Connection Pooling** - چندین اتصال همزمان برای load balancing  
✅ **پروفایل‌های آماده** - Balanced, Aggressive, Latency, CPU-Efficient

---

## ⚡ ویژگی‌ها

### 🌐 پروتکل‌های پشتیبانی شده

| پروتکل | توضیحات | کاربرد |
|--------|---------|--------|
| **TCP** (tcpmux) | پایدار و سازگار | استفاده عمومی |
| **KCP** (kcpmux) | UDP-based، سریع | شبکه‌های پرافت |
| **WebSocket** (wsmux) | عبور از فایروال | محیط‌های محدود |
| **WSS** (wssmux) | WebSocket + TLS | امنیت بالا |

### 🎨 پروفایل‌های عملکرد

| پروفایل | تاخیر | CPU | پهنای باند | کاربرد |
|---------|-------|-----|-----------|--------|
| **balanced** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | استفاده روزمره |
| **aggressive** | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | انتقال فایل |
| **latency** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | بازی، VoIP |
| **cpu-efficient** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | سرورهای ضعیف |

### 🔧 قابلیت‌های پیشرفته

- ✨ **Stream Multiplexing** - چندین stream روی یک اتصال
- 🔄 **Load Balancing** - توزیع بار بین session‌ها
- 🛡️ **PSK Encryption** - رمزنگاری با Pre-Shared Key
- 📊 **Connection Pooling** - بهینه‌سازی تعداد اتصالات
- 🎯 **UDP Flow Management** - مدیریت هوشمند جریان UDP
- 📈 **Auto Buffer Tuning** - تنظیم خودکار بافرها
- 🔁 **Graceful Restart** - راه‌اندازی مجدد بدون قطعی

---

## 🚀 نصب سریع

### نصب خودکار توسط اینستالر 

```bash
wget -O setup.sh https://raw.githubusercontent.com/itsFLoKi/DaggerConnect/main/setup.sh
chmod +x setup.sh
bash setup.sh
```
### سرور ایران
```bash
1- گزینه 1 رو انتخاب کنید 
2- حالا گزینه ای که مدنظرتون هست برای پروتکل انتخاب کنید 
3- پورت تانل مابین دو سرور رو وارد کنید ، مثال 443
4- رمز ارتباط بین دو سرور رو وارد کنید ، در هردو سرور باید یکسان باشد !
5- پروفایل مورد نظر خودتون رو انتخاب کنید
6- پروتکل مپ رو تایپ کنید (tcp,udp)
7- پورت بایند رو وارد کنید این بخش مربوط به پورتی میشود که کاربر درخواست میفرستد ، میتواند باپورت تارگت یکی باشد
8- پورت تارگت رو وارد کنید ، پورتی هست که کانفیگ یا اسکریپت شما در سرور خارج به آن گوش میدهد
9- میتوانید مپ دیگه ای ادد کنید با (y,n)
10- میتوانید لاگ پیشرفته فعال کنید که پیشنهاد نمیشود
!! تمام سرویس شما در سرور ایران نصب شد !!
```
### سرور خارج
```bash
1- گزینه 2 رو انتخاب کنید 
2- رمزی که برای سرور ایران وارد کردید اینجا هم وارد کنید
3- پروفایلی که در سرور ایران وارد کردید اینجا هم وارد کنید
4- حالا گزینه ای که در سرور ایران برای پروتکل انتخاب کردید وارد کنید
5- آیپی سرور + پورت رو وارد کنید ، مثال : 1.2.3.4:443
6- تعداد کانکشن های بین دو سرور رو وارد کنید ، پیشفرض پیشنهاد میشود که اینتر بزنید یا 2 وارد کنید
7- میتوانید پچ دیگه ای ادد کنید با (y,n)
8- میتوانید لاگ پیشرفته فعال کنید که پیشنهاد نمیشود
!! تمام سرویس شما هم در سرور خارج نصب شد !!
```
### جمع بندی نصب
```bash
!!!شما میتوانید در منوی اصلی با وارد کردن گزینه 3 وارد تنظیمات شوید !!!
```

---

## 📚 راهنمای استفاده

### 🔐 مثال پیکربندی اولیه

#### Server (ایران)

```yaml
mode: "server"
listen: "0.0.0.0:PORT_TUNNEL" # در اینجا جای پورت تانل پورت ارتباط بین دو سرور وارد کنید
transport: "tcpmux"
psk: "your-secret-key-here"
profile: "latency"
verbose: false

maps:
  - type: "tcp"
    bind: "0.0.0.0:BIND_PORT" #این پورتی هست که کاربر درخواست میفرسته بهش مثلا پورت 8080
    target: "127.0.0.1:PORT_SCRIPT" # این پورتی هست که به پورت سرویس مربوط میشود ، مثلا پورت کانفیگ شما 6003 هست
```

#### Client (سرور خارج)

```yaml
mode: "client"
psk: "your-secret-key-here"
profile: "latency"
verbose: false

paths:
  - transport: "tcpmux"
    addr: "IP:PORT_TUNNEL" # در اینجا جای پورت تانل پورت ارتباط بین دو سرور وارد کنید و جای آیپی هم آیپی سرور ایرانتون رو وارد کنید
    connection_pool: 4
    retry_interval: 3
    dial_timeout: 10
```

---

## 💡 مثال‌ها

### مثال 1: V2ray

**V2RAY:** 

```yaml
# server.yaml (سرور ایران)
maps:
  - type: "tcp"
    bind: "0.0.0.0:8080"
    target: "127.0.0.1:8080"
```
```bash
# client.yaml (سرور خارج)
# پروتکل مورد نظر باید روی 8080 تنظیم شود
```

### مثال 2: WireGuard Tunnel

**Scenario:** تانل WireGuard از طریق DaggerConnect

```yaml
# server.yaml (سرور ایران)
maps:
  - type: "udp"
    bind: "0.0.0.0:51820"
    target: "127.0.0.1:51820"
```

```bash
# client.yaml (سرور خارج)
# WireGuard باید روی پورت 51820 اجرا شود
```

### مثال 3: Web Server Reverse Proxy

```yaml
# server.yaml
maps:
  - type: "tcp"
    bind: "0.0.0.0:80"
    target: "127.0.0.1:8080"
  - type: "tcp"
    bind: "0.0.0.0:443"
    target: "127.0.0.1:8443"
```

### مثال 4: Multi-Path Setup (Load Balancing)

```yaml
# client.yaml
paths:
  - transport: "kcpmux"
    addr: "server1.example.com:2020"
    connection_pool: 3
  - transport: "tcpmux"
    addr: "server2.example.com:2020"
    connection_pool: 2
  - transport: "wsmux"
    addr: "server3.example.com:8080"
    connection_pool: 2
```

### مثال 5: Gaming Server (Low Latency)
```bash
# (Dns Server)
```
```yaml
mode: "server"
transport: "kcpmux"
profile: "latency" 

maps:
  - type: "udp"
    bind: "0.0.0.0:27015"  # CS:GO
    target: "127.0.0.1:27015"
  - type: "tcp"
    bind: "0.0.0.0:27015"
    target: "127.0.0.1:27015"
```

---

## ⚙️ پیکربندی پیشرفته

### 🎛️ تنظیمات SMUX

```yaml
smux:
  keepalive: 10          # Keepalive interval (seconds) (فاصله زمانی ارسال پیام حفظ اتصال به ثانیه)
  max_recv: 16777216     # 16MB receive buffer (حداکثر اندازه بافر دریافت کل بر حسب بایت)
  max_stream: 16777216   # 16MB stream buffer (حداکثر اندازه بافر اختصاص یافته به هر جریان داده بر حسب بایت)
  frame_size: 32768      # 32KB frame size (حداکثر اندازه فریم SMUX بر حسب بایت)
  version: 2             # SMUX protocol version (استفاده از نسخه 2 پروتکل SMUX)
  mux_con: 10            # SMUX connection (حداکثر تعداد اتصالات همزمان در مالتی‌پلکسینگ)
```

### 🚀 تنظیمات KCP (برای kcpmux)

```yaml
kcp:
  nodelay: 1        # 0=disable, 1=enable (تأخیر در دریافت پکت را حذف می‌کند)
  interval: 10      # Update interval (ms) (فاصله زمانی برای به‌روزرسانی وضعیت KCP)
  resend: 2         # Fast resend (فعال‌سازی مکانیسم ارسال سریع مجدد)
  nc: 1             # No congestion control (غیرفعال کردن کنترل ازدحام، مناسب برای استفاده با SMUX)
  sndwnd: 2048      # Send window (اندازه پنجره ارسال بر حسب تعداد پکت)
  rcvwnd: 2048      # Receive window (اندازه پنجره دریافت بر حسب تعداد پکت)
  mtu: 1300         # Maximum transmission unit (حداکثر اندازه واحد انتقال بر حسب بایت)
  datashard: 10     # تعداد قطعات تقسیم شده برای هر بسته داده اصلی
  parityshard: 3    # تعداد پکت‌های کمکی (FEC) برای هر دسته داده (10 داده + 3 کمکی)
  acknodelay: false # عدم ارسال فوری تأییدیه (ACK) برای بهینه‌سازی سربار
```

#### 🎯 توصیه‌های KCP:

| شرایط شبکه | nodelay | interval | window | MTU |
|------------|---------|----------|--------|-----|
| Stable, Low Loss | 0 | 20 | 512 | 1400 |
| Normal | 1 | 10 | 1024 | 1350 |
| High Latency | 1 | 10 | 2048 | 1300 |
| Packet Loss | 1 | 8 | 2048 | 1200 |

### 🔧 تنظیمات پیشرفته

```yaml
advanced:
  tcp_nodelay: true                # Disable Nagle's algorithm (غیرفعال کردن الگوریتم ناگل برای ارسال فوری بسته‌های TCP)
  tcp_keepalive: 30                # TCP keepalive (seconds) (زمان انتظار برای ارسال پکت Keepalive در TCP بر حسب ثانیه)
  tcp_read_buffer: 16777216        # 16MB (اندازه بافر خواندن TCP بر حسب بایت - 16 مگابایت)
  tcp_write_buffer: 16777216       # 16MB (اندازه بافر نوشتن TCP بر حسب بایت - 16 مگابایت)
  websocket_read_buffer: 262144    # 256KB (بافر خواندن برای ترنسپورت WebSocket)
  websocket_write_buffer: 262144   # 256KB (بافر نوشتن برای ترنسپورت WebSocket)
  websocket_compression: false     # WS compression (غیرفعال کردن فشرده‌سازی WebSocket)
  cleanup_interval: 60             # Cleanup interval (seconds) (فاصله زمانی برای پاکسازی منابع غیرفعال بر حسب ثانیه)
  session_timeout: 180             # Session timeout (زمان انقضای سشن بر حسب ثانیه)
  connection_timeout: 600          # Connection timeout (زمان انتظار برای برقراری اتصال اولیه بر حسب ثانیه)
  stream_timeout: 21600            # Stream timeout (حداکثر زمان عمر یک جریان داده بر حسب ثانیه)
  stream_idle_timeout: 600         # Stream idle timeout (حداکثر زمان بیکار بودن یک جریان قبل از بسته شدن توسط SMUX بر حسب ثانیه)
  max_connections: 0               # Max concurrent connections (0 به معنای نامحدود یا تنظیم شده توسط سیستم/SMUX)
  max_udp_flows: 10000             # Max UDP flows (حداکثر تعداد جریان‌های UDP فعال همزمان)
  udp_flow_timeout: 600            # UDP flow timeout (seconds) (زمان انقضای یک جریان UDP فعال بر حسب ثانیه)
```

---

## 🎮 Systemd Service

### Server Service

```bash
sudo systemctl start DaggerConnect-server
sudo systemctl enable DaggerConnect-server
sudo systemctl status DaggerConnect-server
```

### Client Service

```bash
sudo systemctl start DaggerConnect-client
sudo systemctl enable DaggerConnect-client
sudo systemctl status DaggerConnect-client
```

### نمایش Logs

```bash
# Server logs
journalctl -u DaggerConnect-server -f

# Client logs
journalctl -u DaggerConnect-client -f

# Last 100 lines
journalctl -u DaggerConnect-client -n 100
```

---

## 📊 بنچمارک و عملکرد

### مقایسه پروتکل‌ها

| پروتکل | تاخیر (ms) | پهنای باند (Mbps) | CPU (%) | حافظه (MB) |
|--------|-----------|------------------|---------|-----------|
| **tcpmux** | 15 | 850 | 8 | 45 |
| **kcpmux** | 12 | 920 | 15 | 65 |
| **wsmux** | 18 | 780 | 10 | 50 |
| **wssmux** | 20 | 750 | 12 | 55 |

*تست شده با connection pool=4, profile=latency, شبکه 1Gbps*

### مقایسه پروفایل‌ها (KCP)

| پروفایل | تاخیر (ms) | Throughput (Mbps) | CPU (%) |
|---------|-----------|-------------------|---------|
| **cpu-efficient** | 25 | 450 | 5 |
| **balanced** | 15 | 750 | 10 |
| **latency** | 8 | 900 | 18 |
| **aggressive** | 10 | 950 | 22 |

## 🔒 امنیت

### 1️⃣ PSK قوی استفاده کنید

```bash
# تولید PSK تصادفی
openssl rand -base64 32
```

### 2️⃣ استفاده از WSS برای رمزنگاری

```yaml
transport: "wssmux"
cert_file: "/etc/ssl/cert.pem"
key_file: "/etc/ssl/key.pem"
```

### 3️⃣ محدود کردن دسترسی

```bash
# فقط IP خاص
sudo ufw allow from 1.2.3.4 to any port 2020
```

### 4️⃣ Rate Limiting

```yaml
advanced:
  max_connections: 1000
  max_udp_flows: 500
```

---

## 📈 نظارت (Monitoring)

### Prometheus Metrics (TODO)

```yaml
metrics:
  enabled: true
  port: 9090
```

### Health Check

```bash
# چک سلامت server
curl http://server-ip:9090/health

# چک تعداد session ها
curl http://server-ip:9090/metrics | grep sessions
```

---

## 🛠️ ابزارهای کمکی

### تست سرعت

```bash
# نصب iperf3
sudo apt install iperf3

# سرور
iperf3 -s -p 5201

# کلاینت (از طریق تانل)
iperf3 -c tunnel-ip -p 5201
```

### نظارت بر ترافیک

```bash
# نمایش ترافیک لحظه‌ای
iftop -i eth0

# آمار ترافیک
vnstat -l
```

---

## 📝 لایسنس

«این پروژه تحت مجوز انحصاری منتشر شده است و متن‌باز نیست (یا: تمامی حقوق آن محفوظ است).»

---

## 🙏 تشکر

- [xtaci/smux](https://github.com/xtaci/smux) - Stream multiplexing
- [xtaci/kcp-go](https://github.com/xtaci/kcp-go) - KCP protocol
- [gorilla/websocket](https://github.com/gorilla/websocket) - WebSocket

---

## 📞 پشتیبانی

- 🐛 [Report Bugs](https://github.com/itsFLoKi/DaggerConnect/issues)
- 💬 [Discussions](https://github.com/itsFLoKi/DaggerConnect/discussions)
- 📧 Telegram : @CentralFourOne

---

<div align="center">

⭐ اگه مفید بود یه ستاره بدید!

[⬆ برگشت به بالا](#-DaggerConnect)

</div>